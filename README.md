# World-wide-layoffs-analysis-using-SQL
This project cleans and explores a layoffs dataset using SQL. Starting with cleaning data by removes duplicates with window functions, standardizes key fields and deletes rows that contain no layoff information. After cleaning, the analysis explores layoffs across companies, industries, funding levels, stages, and time.
